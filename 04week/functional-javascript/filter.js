var arrayStrings = [
    {
        message1: 'Drop in wax kingpin baseplate Mark GonzalesDisaster nose bump mini ramp rail slide full-cab Steve Rocco. Wax flail face plant powerslide mini ramp.'
    },
    {
        message1: 'Grab Saran Wrap Rodney Mullen'

    },
    {
        message1: 'Tracker Andy Levy nose blunt crailtap'
    },
    {
        message1:  'hip Christ air. Bail drop in Andy Takakjian'
    },
    {
        message1: 'egg plant wall ride.'
    },
    {
        message1: 'Sponsored roll-in The Wedge rail slide full pipe chicken wing. Steve Olson fastplant flypaper full pipe 270 powerslide.'
    }
];
// function getShortMessages(messages) {//function with messages the argument
//   let filt = messages.filter(function(mess) {//variable filt with the argrument .filter where  a new function as mess the new function argument
//   return mess.message1.length < 50 //returning the argument.message1.length <50

//   });
//   return filt.map(function(mess){ //notice how if I only return this array of undefined.
//     return mess.message1;
//   })
// }console.log()
// getShortMessages(arrayStrings);

// // module.exports = getShortMessages;